spring:
  datasource:
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: DEBUG
    org.springframework.security: TRACE
